{
  "name": "finance-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private, offline personal finance tracker. No dependencies by design.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "python3 build.py",
    "serve": "python3 -m http.server 8080"
  }
}
